---
import {
  LandingLayout,
  HeroWall,
  CatalogSection,
  FlowSection,
  CtaSection,
  loadStats
} from "@/features/landing"

const stats = await loadStats()
---

<LandingLayout>
  <HeroWall stats={stats} />
  <CatalogSection stats={stats} />
  <FlowSection />
  <CtaSection />
</LandingLayout>
